## Simple react application (docker-compose)

---
### To run image:
`docker-compose up`

### To stop the image:
`docker-compose down`

### To rebuild the image:

`docker-compose up --build`

### Note
you can specify app port mapping in `docker-compose.yml`
